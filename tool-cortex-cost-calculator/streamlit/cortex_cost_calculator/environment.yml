# Snowflake environment descriptor for Streamlit in Snowflake (SiS)
# This file specifies Python packages needed for the Cortex Cost Calculator v3.3
#
# Version constraints provide reproducibility while allowing minor updates.
# SiS manages compatibility; these are minimum versions for feature requirements.

name: cortex_cost_calculator
channels:
  - snowflake

dependencies:
  - streamlit
  - snowflake-snowpark-python
  - pandas
  - numpy
  - plotly
