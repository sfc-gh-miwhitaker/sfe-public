install:
  commands:
    - [npm, install]
build:
  commands:
    - [npm, run, build]
profile:
  icon: logo.svg
  label: AI Cost Controls
  description: Read-only dashboard for Cortex AI spend attribution, quota status, and trend analysis.
